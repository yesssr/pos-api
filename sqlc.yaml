version: "2"
sql:
  - engine: "postgresql"
    schema: "db/migrations/*.sql"
    queries: "db/queries/*.sql"
    gen:
      go:
        out: "internal/store"
        package: "store"
        sql_package: "pgx/v5"
        emit_json_tags: true
